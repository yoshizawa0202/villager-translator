# コントリビューションガイド

Villager Translator のブランチ運用、プルリクエスト（PR）、リリース手順を定めます。アプリの機能仕様は `docs/specs/`、GitHub Actions の詳細は `docs/workflows.md` を参照してください。

## ブランチ構成

| ブランチ | 役割 | 運用ルール |
|---|---|---|
| `main` | リリース済みの安定版 | 直接 push せず、原則として `dev` からのリリースPRをマージする |
| `dev` | 開発内容の統合先 | 通常のPRはこのブランチをマージ先にする |
| `feat/*` | 機能追加 | `dev` から分岐し、`dev` へPRを作成する |
| `fix/*` | 不具合修正 | `dev` から分岐し、`dev` へPRを作成する |
| `docs/*` | 文書のみの変更 | `dev` から分岐し、`dev` へPRを作成する |
| `chore/*` | CIや開発環境などの保守 | `dev` から分岐し、`dev` へPRを作成する |
| `release/vX.Y` | 過去バージョンの保守 | hotfix が必要な場合のみ、対象のリリースタグから作成する |

GitHub のデフォルトブランチは `dev` とします。`main`、`dev`、`release/*` にはブランチ保護ルールを適用し、PR、メンテナーレビュー、必須CIの成功をマージ条件とします。force-push とブランチ削除は行いません。

これは目標とする運用です。文書のマージだけではGitHub設定は変わりません。導入時は `dev` 向けPRでCI成功を確認し、必須チェックに `解析・テスト・Windows ビルド` を設定してから、デフォルトブランチを `dev` に変更してください。パス条件で実行が省略される Claude Code Review は全PR共通の必須チェックにしません。

## 通常の開発手順

1. 最新の `dev` を取得する。
2. 変更内容に合った作業ブランチを `dev` から作成する。
3. 機能変更では、実装前に `docs/specs/` の仕様と受け入れ条件を追加または更新する。
4. 実装、テスト、関連文書を同じ変更に含める。
5. 必要なローカル検証を実行する。
6. 作業ブランチを push し、`dev` 向けのPRを作成する。
7. レビューと必須CIが完了してからマージする。

### ブランチ作成例

```powershell
git switch dev
git pull --ff-only origin dev
git switch -c feat/issue-123
```

## PRのルール

- PR本文に目的、主な変更点、検証結果、未確認事項を記載する。
- 関連するIssueがある場合は、PR本文から参照する。
- アプリの挙動を変更する場合は、対応する仕様書とテストを含める。
- `Windows CI/CD` が成功していることを確認する。
- `main` 向けPRはリリース用または承認済みhotfixに限定する。
- `dev` から `main` へのリリースPRは通常の merge（Create a merge commit）を使い、共通の履歴を保つ。長期運用するブランチ間の squash merge は避ける。

## ローカル検証

変更内容に応じて、次のコマンドを実行します。

```powershell
dart format .
flutter analyze
flutter test
flutter build windows --release
```

文書やGitHub Actionsだけの変更では、アプリの挙動に影響しないことを確認したうえで、変更対象に必要な検証へ限定できます。その場合は、実行しなかった検証をPR本文に明記します。

## 通常リリース

リリース作業はメンテナーレベルのメンバーが行います。

1. `dev` の必須CIが成功していることを確認する。
2. Windows実機でリリース対象の主要動作を確認する。
3. `dev` からリリース準備ブランチを作成する。
4. `pubspec.yaml` の `version` を `X.Y.Z+N` 形式で更新する。`N` は過去のビルド番号より大きくする。
5. バージョン変更を `dev` 向けPRでレビューし、マージする。
   バージョン変更後のコミットでCIとWindows実機確認を行い、リリースPRの対象を確定する。
6. `dev` から `main` へのリリースPRを作成する。PR本文に変更点、検証結果、既知の制限を記載する。
7. メンテナーレビューと必須CIの成功後にマージする。
8. マージ後の `main` コミットへ、`pubspec.yaml` と一致する `vX.Y.Z` タグを作成して push する。
9. `Windows CI/CD` の `verify` と `release` が成功し、GitHub Release に `VillagerTranslator-windows-x64.zip` が添付されたことを確認する。
10. `main` から `dev` への同期PRを通常の merge で取り込み、次の開発に備える。

タグを作成する前に、対象コミットが `main` の最新コミットであることを確認します。公開済みタグを別のコミットへ付け替えません。

`pubspec.yaml` には `v` を付けず、タグにはビルド番号 `+N` を含めません。既存の公開タグより新しいSemVerを選びます。現行CIはタグの所属ブランチやバージョン一致を自動検証しないため、メンテナーが確認します。

## Hotfix

通常リリースでは `release/*` ブランチを作成しません。過去のリリース系列を修正する必要がある場合だけ、対象タグから `release/vX.Y` を作成します。

現行CIのブランチ対象は `main` と `dev` のみです。保守運用を始める前に、対象系列のワークフローをPRで更新して `release/*` 向けPR・pushを対象に加え、CIが実行されることを確認してください。過去タグには古いワークフローが含まれる点に注意します。ブランチ作成制限がある場合は権限を持つメンテナーが作成します。

1. 対象のリリースタグから `release/vX.Y` を作成する。
2. `release/vX.Y` から `fix/*` を作成し、修正と `pubspec.yaml` のパッチバージョン・ビルド番号更新を含む `release/vX.Y` 向けPRを作成する。
3. レビュー、CI、Windows実機確認後にマージする。
4. 検証済みの保守ブランチのマージコミットへ未使用の `vX.Y.Z` タグを作成・pushし、Releaseと添付ZIPを確認する（通常リリースの `main` 上でのタグ作成に対する例外）。
5. 同じ修正が現行開発にも必要な場合は、別PRで `dev` に反映する。
